# V23.63.18

Scope: Idefix + Apple + iPhone + v23.63.17 curated landing candidate only.

When the source canonical query has no explicit marketed network, and the curated candidate card has exact family + variant + storage but a standalone 5G badge, the badge is removed only from the search-card scoring copy. The original label, URL, detail page, canonical matcher, price integrity and fail-closed behavior remain unchanged.
