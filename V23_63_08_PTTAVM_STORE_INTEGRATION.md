# V23.63.08 PttAVM Store Integration

PttAVM was promoted from onboarding to active product scraping. Search uses `/arama?q=...`; product URLs use root slugs ending in `-p-<id>`. Existing canonical identity, network, storage, color and price-integrity gates remain fail-closed. Teknosa was already active and is intentionally preserved without behavior changes.
