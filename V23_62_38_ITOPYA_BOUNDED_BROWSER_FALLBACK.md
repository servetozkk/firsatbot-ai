# FirsatAI v23.62.38

Itopya unhealthy HTTP-first responses (including 404) remain non-decisive. Browser fallback is preserved but bounded to a 5s navigation budget plus a 1s real-product-anchor readiness probe. Only `/urun/` or `_u` product links permit normal candidate extraction. No anchor means fail-closed NO_CANDIDATE; identity, price and production ingestion behavior are unchanged.
