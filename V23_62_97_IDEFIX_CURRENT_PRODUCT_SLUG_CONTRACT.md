# V23.62.97

Idefix current product URLs use root-level slugs ending in `-p-<id>` rather than the legacy `/urun/` path.

This release preserves v23.62.96 strong-query and bounded-search behavior, while extending only the Idefix adapter/readiness/extraction URL contract to support the current root product slug. Legacy `/urun/` support remains.
